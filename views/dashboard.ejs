<%- include('partials/header') %>
  <div class="container mt-5">
    <h1>Welcome <%= user %></h1>
    <br>
    <h3>Let's Complete KYC</h3>
    <br>
    <div class="internal-container">
        <h2>What is KYC ?</h2>
        <p>

            KYC means Know Your Customer and sometimes Know Your Client.
            
            KYC or KYC check is the mandatory process of identifying and verifying the client's identity when opening an account and periodically over time.
            
            In other words, banks must ensure that their clients are genuinely who they claim to be.

            Banks may refuse to open an account or halt a business relationship if the client fails to meet minimum KYC requirements
        </p>
    </div>
    <br><br><br>
    <h2>Your KYC Status</h2>
    <br>
    <br>
    <% if (locals.complete && complete==true) {%>
        <p style="text-align: center; display: block; font-size: 20px; border: 3px solid black; background-color: green;">Complete ✅</p>
        <p class="verified-docs">Photo Verification ✅</p>
        <p class="verified-docs">Adhaar Verification ✅</p>
        <p class="verified-docs">PAN Verification ✅</p>
    <%} else {%>
        <p style="text-align: center; display: block; font-size: 20px; border: 3px solid black; background-color: #FFCCCB;">Incomplete</p>
        <% if (locals.photo && photo==true) {%>
            <p class="verified-docs">Photo Verification ✅</p>
        <%} else {%>
            <form action="/camera" method="GET">
                <button class="unverified-docs" onclick="submit">Verify Photo</button>
            </form>
        <%}%>

        <% if (locals.adhaar && adhaar==true) {%>
            <p class="verified-docs">Adhaar Verification ✅</p>
        <%} else {%>
            <form action="/adhaar" method="GET">
                <button class="unverified-docs" onclick="submit">Verify Adhaar</button>
            </form>
        <%}%>

        <% if (locals.pancard && pancard==true) {%>
            <p class="verified-docs">PAN Verification ✅</p>
        <%} else {%>
            <form action="/pan" method="GET">
                <button class="unverified-docs" onclick="submit">Verify PAN</button>
            </form>
        <%}%>
    <%}%>
  </div>

  <%- include('partials/header') %>